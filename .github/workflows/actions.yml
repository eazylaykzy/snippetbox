name: Snippetbox Actions

on:
  push:
    branches: [ master ]
    pull_request:
      branches: [ master ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.17
      - name: "Mysql Setup"
        run: |
          sudo /etc/init.d/mysql start
          mysql -e 'CREATE DATABASE ${{ secrets.DB_DATABASE }} CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;' -u${{ secrets.DB_USER }} -p${{ secrets.DB_PASSWORD }}
          
          USE snippetbox;
          
          CREATE TABLE snippets (
          id INTEGER NOT NULL PRIMARY KEY AUTO_INCREMENT,
          title VARCHAR(100) NOT NULL,
          content TEXT NOT NULL,
          created DATETIME NOT NULL,
          expires DATETIME NOT NULL
          );
          
          CREATE INDEX idx_snippets_created ON snippets(created);
          
          CREATE USER 'web'@'localhost';
          GRANT SELECT, INSERT, UPDATE ON snippetbox.* TO 'web'@'localhost';
          ALTER USER 'web'@'localhost' IDENTIFIED BY '1234';
      - name: Build
        run: go build -v ./...

  test:
    runs-on: ubuntu-latest
    needs: [ build ]
    steps:
      - uses: actions/checkout@v2

      - name: "Mysql Setup"
        run: |
          sudo /etc/init.d/mysql start
  
          mysql -e 'CREATE DATABASE ${{ secrets.TEST_DB_DATABASE }} CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;' -u${{ secrets.DB_USER }} -p${{ secrets.DB_PASSWORD }}
          
          CREATE USER '${{ secrets.TEST_DB_DATABASE }}'@'localhost';
          GRANT CREATE, DROP, ALTER, INDEX, SELECT, INSERT, UPDATE, DELETE ON ${{ secrets.TEST_DB_DATABASE }}.* TO '${{ secrets.TEST_DB_USER }}'@'localhost';
          ALTER USER '${{ secrets.TEST_DB_USER }}'@'localhost' IDENTIFIED BY '${{ secrets.DB_TEST_PASSWORD }}';
      - name: Test
        run: go test -v ./...